##  今天和昨天都是`php`和`java`
昨是html5明天也是html5，但是我昨天的html5我已经上传了
明天的`html5`还没有上今天写一些php与java
现在学的东西太杂了感觉想把一个学好还得带着很多的相联系的东西，也感觉一天是很累的
不过还得学啊，每天都上传
```
定义了两个伪类java但是伪类可以随意命名:
student,是第二伪类；
第一个伪类为：hai，
用hai伪类来调用student
private String name;
private int age;
public String getName() {
    return name;
} 
public void main(String stuName) {
    name stuName;
}
public int getAge() {
    return Age;
}
public void setAge(int stuAge)  {
    if (stuAge<=0) {
        System.out.println("对不起");
    }else{
        age=stuAge;
    }
}
public void introduce() {
    System.out.println("大家好"+name+"今年"+age+"岁");
}
}
public static void main(String[]args) {
    Student stu=new Student();
    stu.setAge(-30);
    stu.setName("丁海超")；
    stu.intorduce();
}
用后一个来调用上一个这样是非常好用的
这个今天学的java
```
<?php
function king ($n,$v) {
    $monkey=range(1,$m);
    $i=0;
    while(count($monkey)>1) {
        ++$i;
        $head=array_shift($monkey);
    }
}
return['total'=>$n,kick=>$m,'king'=>$m,'king'=>$monkey[0]];
$data=king(10,7);
<table>
<tr><th colspan="2">找猴王游戏</th></tr>
<tr><td>猴子总数</td><td><?=$data['total']?></td></tr>
<tr><td>踢出第m个猴子</td><td><?=$data['kick']?></td></tr>
<tr><td>猴子编号</td><td><?=$data['king']?></td></tr>
</table>
这个是找猴王游戏，随机找猴王游戏，
?>
```

----
其实把我每天都写这些东西也感觉自己也在长知识，
学了这么多的编程语言我还是喜欢`html5`,这个里面还添加了`js`,`css`,感觉`html5`真的感性趣。
也很有意思，不写了，
加油。