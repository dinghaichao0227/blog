## css笔记
笔记上也有很多要点，有点要点是必须记下来的我要记的：
```
css 的过度transition属性是一个复合属性，
transition-property：指完成过渡或动态模拟的css属性
transition-duration：指过渡所需要的时间
transition-timg-function：指定过度函数
transition-delay：指定开始出现的延迟时间
这是一种过渡复合属性的状态如动漫设计
长css背景属性
background：简写属性，作用是将背景属性设置在一个声明中
background-attachment： 背景图片是否固定或者随着页面的其余部分滚动
background-color：设置元素的背景颜色
background-image：把图片设置为背景
background-position：设置背景图片的起始位置
background-repeat：设置背景图片是否及如何重复
这些是如何设置背景的方法
css背景属性的样式
color：设置文本颜色
direction：设置文本方向
letter-spacing：设置字符间距
text-align：对齐元素中的文本
text-indent：缩进元素文本的首行
text-decoration：向文本添加修饰
text-shadow：设置文本阴影
text-transform：控制元素中的字母
Unicode-bidi：设置或返回文本是否重写
vertical-align：设置元素的垂直对齐
white-space：设置元素中空白的处理方式
word-spacing：设置字间距
修饰背景的方式
css修饰字体：
font：在一个声明中设置所有的字体属性
font-family：指定文本的字体序列
font-size：指定文本的字体大小
font-style：指定文本的字体样式
font-variant：以小型大写字体或正常字体显示文本
font-weight：指定字体的粗细
字体的修饰
css列表：
list-style：用于把所有用于列表的属性设置一个声明中
list-style-image：将图片设置为列表项标志
list-style-position：设置列表中的列表项标志的位置
list-style-type: 设置列表项标志的类型
列表属性。
css边框：border，
none默认没有边框
dotted：点线边框
dashed：虚线边框
solid：实线边框
double：两个边框，两人边框宽度和border-width相等
groove：3d沟槽边框
ridge：3d脊边框
inset：3d的嵌入边框
outset：3d突出边边框
边框的属性。
边框的修饰：
border-style：设置所有边框的样式或单独设置
border-width：设置所有边框宽度或单独设置
border-color：设置所有边框的颜色或单独设置
border-bottom：设置边框底部所有属性
border-bottom-color：边框底部颜色
border-bottom-width：边框底部宽度
border-bottom-style：边框底部样式
border-left：设置边框左部
border-left-color同上
border-left-width
border-left-style
border-right：设置边框右部
border-right-color：同上
border-right-width
border-right-style
border-top：设置边框的上部
border-top-color：同上
border-top-width
border-top-style
。
css的轮廓
outline：设置所有的轮廓
属性值
outline-color
outline-width
outline-style
inherit
outline-color：设置轮廓的颜色
属性值
color-name
hex-number
rgb-number
invert
inherit
outline-style：设置轮廓样式
属性值
none
dotted
dashed
solid
dashed
groove
ridge
inset
outset
inherit
outline-width：设置轮廓的宽度
属性值
thin
medium
thick
length
inherit
。
嵌套选择器：
p{}指定所有样式
.marked{}：为所有class="marked"元素指定样式
.marked p{}：为所有class="marked"元素内的p元素指定样式
p.marked{}: 为所有class="marked"的p元素指定样式
```