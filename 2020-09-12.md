## 最近一周的笔记
上了一个周的`java` `php` `mysql` `html5`
一周学了四种课每一个都是基本知识
```
其中java就是用基本的案例来进行讲授但是其中也有些基本的语法
package woshinidie;
public class nishiwoerzi {
	String username;
	String _username;
	String $username;
	String username_;
     public static void main(String[] args){
    	 System.out.println("这是一个注释");
    	 int x = 8;
    	 System.out.println("X="+x);
    	 int c = 8;
    	 int y = x+c;	 
    	 System.out.println("y="+y);
    	 
    	 char  a='c';
    	 System.out.println("a="+a);
    	 char  A=99;
    	 System.out.println("A="+A);
    	 }
    	 
    	 public static void main(String[] args){
    		 int num = 4;
    		 byte b = (byte) num;
    		 System.out.println("b="+b);
    		 byte a;
    		 int c = 298;
    		 a = (byte)c;
    		 System.out.println("c="+c);
    		 System.out.println("a="+a);
    		 }
			public static void main(String[] args){
				int x = 10;
				int y = 3;
				System.out.println("x"+"加"+"y="+(x+y));
				System.out.println("x"+"减"+"y="+(x-y));
				System.out.println("x"+"乘"+"y="+x*y);
				System.out.println("x"+"除"+"y="+x/y);
				float a = 3.0f;
				System.out.println("x/a="+x/a);
				double b = 3.0d;
				System.out.println("x/b="+x/b);
				
				int p = 1;
				int q = 3;
				int w = p + q++;
				System.out.println("p="+p);
				System.out.println("q="+q);
				int r = (++q);
				System.out.println("r="+r);
				int d = p/q;
				System.out.println("d="+d);
				int f = p%q;
				System.out.println("f="+f);
				
			}
}
这些是Java的一周所学的东西感觉还是有点小难度的每一个语法都有固定的意思，
而html5中学了三个部分
第一部分就是用最少的代码来实现表单的形成里面有了Java中的知识
第二部分也是用了Java的知识来测验是否是现在所学的用代码最少的
第三部分是用Java中的 方式来设置不同的图形
movelo是一种属性是使用制作图形中的修改的样式
linelo是一种用图片移动方向属性
closepath是一种制作图片的路径
stroke是一种制作图片的动作
php是需要安装php环境才能运行没有安装那个环境所有就没有办法运行但是
<?php>也是php中的单标记等等
```