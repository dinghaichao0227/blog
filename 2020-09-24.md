## java知识点
今天学的是`Java`上了一上午的Java。
代码还是跟往常一样都记下来了：开始：
```
public static void main (String[]args){
int[]arr;
arr=new int[2];
System.out.println("arr[0]="+arr[0]);
System.out.println("arr[1]="+arr[1]);
System.out.println("数据长度"+arr.length);
}
int[]arr;
arr=new int[2];
arr(0)=1;
arr(1)=2;
System.out.println("arr[0]="+arr[0]);
System.out.println("arr[1]="+arr[1]);
}
int[]arr={1,2,3,4,5};
System.out.println("arr[0]="+arr[0]);
System.out.println("arr[1]="+arr[1]);
..
int[]arr={2,3,4,5,6};
int max=getMax(arr);
System.out.println("max="+max);
getMax会有重载的效果
for (int s=1;s<arr.length;s++){
    if (arr[i]>max);
    max=arr[s];
}
最大值和最小值和平均值都是这样的但是宿主的不一样
如：
getMax最大值，
getMin最小值；
getAvg平均值；
冒泡排列：
int[]arr={2,3,4,5,6};
System.out.println("冒泡")
printArray(arr);
bubbbleSort(arr);
system.out.print("冒泡后")；
println(arr);
for（int i=0;i<arr.length;i++）{
    System.out.print(arr[i]+'') ;
}
System.out,print('\n');
下面和上面都是一样，
这个也是有重载的方法的；
第一行的是标配Java的开头，
```
今天很少的知识但是我还是写了今天真的很少，
加油！！
再见明天继续；